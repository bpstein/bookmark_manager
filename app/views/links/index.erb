

<div class="container">
  <h1> Welcome to Bookmark Manager </h1>

  <ul id='links'>
    <% @links.each do |link| %>
      <li>
        Title: <%= link.title %>
        URL:   <%= link.url   %>
        Tag:   <%= link.tags.map(&:name)   %>
      </li>
    <% end %>
  </ul>
</div>
